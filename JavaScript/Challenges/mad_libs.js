const name =  prompt("Enter a name"); //person
const adj = prompt("Enter an adjective"); //brave
const noun = prompt("Enter a noun"); //building
const verb = prompt("Enter an verb");//jumping
const place =  prompt("Enter a place");//texas

// console.log("One day, a "+adj+" " + name +" "+verb+" through the "+noun+" in "+place);
const story = `One day, a ${adj} ${name}  ${verb} through the ${noun} in ${place} `;
console.log(story);